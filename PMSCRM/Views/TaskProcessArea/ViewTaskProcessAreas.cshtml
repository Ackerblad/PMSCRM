@model IEnumerable<PMSCRM.ViewModels.TaskProcessAreaDisplayViewModel>

@{
    ViewData["Title"] = "View Task Process Areas";
    Layout = "~/Views/Shared/_Layout.cshtml"; 
}
<link rel="stylesheet" href="~/css/buttons.css">
<link rel="stylesheet" href="~/css/table.css">

<div class="card shadow-sm p-4 mb-4">
    <table class="table-striped table-hover custom-table">
        <thead>
            <tr>
                <th>Area</th>
                <th>Process</th>
                <th>Task</th>
                <th>Date</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var taskProcessArea in Model)
                {
                    <tr>
                        <td>@taskProcessArea.AreaName</td>
                        <td>@taskProcessArea.ProcessName</td>
                        <td>@taskProcessArea.TaskName</td>
                        <td>@taskProcessArea.Timestamp.ToString("g")</td>
                        <td>

                            <a href="/TaskProcessArea/EditTaskProcessArea/@taskProcessArea.TaskProcessAreaId" class="btn btn-edit">Edit</a>
                            <a href="/TaskProcessArea/DeleteTaskProcessArea/@taskProcessArea.TaskProcessAreaId" class="btn btn-delete">Delete</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4" class="text-center">No Task Process Areas found.</td>
                </tr>
            }
        </tbody>
    </table>
    <div>
        <a asp-area="" asp-controller="TaskProcessArea" asp-action="AddTaskProcessArea" class="btn btn-add">Add TPA</a>
    </div>
</div>
